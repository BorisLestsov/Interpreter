#define hello -5
#define hey 5555

program
{

 struct mystruct{
  int i;
  string a = "stst";
 } s;

 struct nextstruct{
 int i = 0;
 bool b = true;
 };

 int a = 60, b = hello, c;
 #undef hello
 string x = "abc", y, z = "hey";
 bool hello = true;


 c = (a+b) * 2; ;
 if (c >= 100 or b == c and (true or a*b<3)){
   c = a / b; 			// hello
    read (y);
    write (y, "\n");
    write (x + y + z, c, "\n");
 } else 
    c = a+ 100; //= 21;
 while (c > 100)
 {
 c = s.i - 5;
 write (c);
 x = x + "step";
 s.a="yo"+z;
 write(s.a, "\n");
 //break;
 }
 goto M;
M:;
}
