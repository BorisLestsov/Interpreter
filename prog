#define hello -5
#define hey 5555

program
{
 struct mystruct{
  int i;
  string a = "stst";
 } s;
 struct nextstruct{
 int i;
 bool b;
 };
 int a = 51, b = hello, c;
 #undef hello
 string x = "abc", y, z = "hey";
 bool hello = true;
 c = (a + b) * 2; if (c >= 100 or x == z)
 {
M:  c = a / b; // hello
 read (y);
 write (y);
 write (x + y + z, c);
 }
 else
 c = a = 21;
 while (c > 100)
 {
 c = c - 5;
 write (c);
 x = x + "step";
 break;
 }
goto M;
 write (x);
}
